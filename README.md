Requires python3.10
# installation
`python3.10 -m pip install -r requirements.txt`

# example
`python3.10 tts.py --help`
`python3.10 tts.py --text "Hi, how are you?" -esl out/out.mp3 -rsl out/out.wav -rm "gura/gura.pth" -i "gura/gura.index" -s 12 -g f`
